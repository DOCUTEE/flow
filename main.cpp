#include <bits/stdc++.h>
using namespace std;

#define int long long
#define MASK(i) (1LL << (i))
const int inf = 1e18;

void solve()
{
      cin >> a >> b;
}
signed main()
{
      ios_base::sync_with_stdio(0);
      cin.tie(0);
      cout.tie(0);
      int t = 1;
      if (t > 1)
            cin >> t;
      while (t--)
      {
            solve();
      }
}
// I will be in OLP HANOI next 6 months
